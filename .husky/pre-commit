#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Note: Node version enforcement disabled for flexibility
# Original enforcement kept Node 18.18.0 but builds work fine with newer versions

npm run lint-staged
